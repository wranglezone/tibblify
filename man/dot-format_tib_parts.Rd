% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format-tib.R
\name{.format_tib_parts}
\alias{.format_tib_parts}
\title{Build the formatted argument string for a collector call}
\usage{
.format_tib_parts(
  f_name,
  x,
  ...,
  .fill = NULL,
  .ptype_inner = NULL,
  .transform = NULL,
  multi_line = FALSE,
  nchar_indent = 0,
  width = NULL,
  names = FALSE
)
}
\arguments{
\item{f_name}{(\code{character(1)}) The (possibly ANSI-colored) function name.}

\item{x}{A tibblify collector object.}

\item{...}{Additional named arguments to include verbatim in the output,
forwarded from the calling \verb{format.*} method.}

\item{.fill}{(\code{vector} or \code{NULL}) Optionally, a value to use if the field
does not exist.}

\item{.ptype_inner}{(\code{vector(0)}) A prototype of the input field.}

\item{.transform}{(\code{function} or \code{NULL}) A function to apply to the whole
vector after casting to \code{.ptype_inner}.}

\item{multi_line}{(\code{logical(1)}) Should the output be formatted across
multiple lines? For example, should each element of even a short list be
displayed on its own line?}

\item{nchar_indent}{(\code{integer(1)}) The number of (additional) characters that
will be used to indent the output when \code{multi_line = TRUE}. Primarily for
internal use when formatting is applied recursively.}

\item{width}{(\code{integer(1)}) The width (in number of characers) of text output
to generate.}

\item{names}{(\code{logical(1)}) Should names be printed even if they can be
deduced from the spec?}
}
\value{
(\code{character(1)}) A string of comma-separated, possibly wrapped
arguments ready to be placed inside the parentheses of the collector call.
}
\description{
Build the formatted argument string for a collector call
}
\keyword{internal}
