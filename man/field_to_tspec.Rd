% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_to_tspec.R
\name{field_to_tspec}
\alias{field_to_tspec}
\alias{field_to_tspec_df}
\alias{field_to_tspec_row}
\alias{field_to_tspec_recursive}
\title{Elevate a field to a tspec}
\usage{
field_to_tspec(spec, name)

field_to_tspec_df(spec, name)

field_to_tspec_row(spec, name)

field_to_tspec_recursive(spec, name)
}
\arguments{
\item{spec}{(\code{tspec}) A tibblify specification.}

\item{name}{(\code{character(1)}) The name of the field.}
}
\value{
A tibblify specification (\code{tspec_df}, \code{tspec_row}, or
\code{tspec_recursive}).
}
\description{
Extract a nested field from a \code{tspec} and convert it to a top-level
\verb{tspec_*()} object. \code{field_to_tspec()} dispatches to the appropriate
variant based on the type of the field. Use \code{field_to_tspec_df()},
\code{field_to_tspec_row()}, or \code{field_to_tspec_recursive()} to extract a field
of a specific type.
}
\examples{
spec <- tspec_df(
  tib_int("id"),
  tib_df(
    "address",
    tib_chr("street"),
    tib_chr("city")
  )
)

field_to_tspec(spec, "address")
field_to_tspec_df(spec, "address")
}
