# Format a fill value as a character string

Format a fill value as a character string

## Usage

``` r
.format_fill(x)
```

## Arguments

- x:

  A fill value.

## Value

(`character(1)`) An R expression string representing `x`.
