/**
 * @file tibblify_types.h
 * @brief Convenience header that pulls in all core tibblify types.
 *
 * Include this header when you need the full type hierarchy. It transitively
 * includes `Path.h` → `tibblify.h` → all R/vctrs/rlang headers.
 */
#include "Path.h"
