# Pad and collapse a character vector across multiple lines

Pad and collapse a character vector across multiple lines

## Usage

``` r
.paste_multiline(x)
```

## Arguments

- x:

  (`character`) A vector to pad and collapse.

## Value

A single string with the elements of `x` padded and collapsed across
multiple lines, with a leading newline and a trailing comma on each
line.
