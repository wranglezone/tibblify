# Is x a list of NULLs?

Is x a list of NULLs?

## Usage

``` r
.is_list_of_null(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if every element of `x` is `NULL`, `FALSE`
otherwise.
