# Format the `.ptype` argument for display

Format the `.ptype` argument for display

## Usage

``` r
.format_ptype_arg(x)
```

## Arguments

- x:

  A tibblify collector object.

## Value

(`character(1)` or `NULL`) The formatted ptype string, or `NULL` if the
argument should be omitted.
