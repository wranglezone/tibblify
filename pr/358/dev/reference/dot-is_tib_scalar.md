# Check if object is a tib scalar

Check if object is a tib scalar

## Usage

``` r
.is_tib_scalar(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if `x` is a `tib_scalar`.
