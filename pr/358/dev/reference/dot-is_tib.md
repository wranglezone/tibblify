# Check if object is a tib collector

Check if object is a tib collector

## Usage

``` r
.is_tib(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if `x` is a `tib_collector`.
