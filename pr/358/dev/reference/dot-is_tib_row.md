# Check if object is a tib row

Check if object is a tib row

## Usage

``` r
.is_tib_row(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if `x` is a `tib_row`.
