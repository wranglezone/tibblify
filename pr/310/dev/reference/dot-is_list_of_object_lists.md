# Is x a list of object lists?

Is x a list of object lists?

## Usage

``` r
.is_list_of_object_lists(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if every non-`NULL` element of `x` is a list of
objects, `FALSE` otherwise.
