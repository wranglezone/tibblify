# Drop NULL elements from a list

Drop NULL elements from a list

## Usage

``` r
.list_drop_null(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

`x` with `NULL` elements removed.
