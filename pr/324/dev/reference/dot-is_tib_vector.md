# Check if object is a tib vector

Check if object is a tib vector

## Usage

``` r
.is_tib_vector(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if `x` is a `tib_vector`.
