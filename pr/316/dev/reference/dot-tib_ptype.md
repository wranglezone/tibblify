# Get the ptype of an object

Get the ptype of an object

## Usage

``` r
.tib_ptype(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

The ptype of `x`, with `POSIXlt` coerced to `POSIXct`.
