# Check if object is a tib variant

Check if object is a tib variant

## Usage

``` r
.is_tib_variant(x)
```

## Arguments

- x:

  (`any`) The object to check.

## Value

(`logical(1)`) `TRUE` if `x` is a `tib_variant`.
