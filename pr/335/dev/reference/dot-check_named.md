# Abort for missing names

Abort for missing names

## Usage

``` r
.check_named(x, call)
```

## Arguments

- x:

  (`any`) The object to check.

- call:

  (`environment`) The environment to use for error messages.

## Value

`NULL` (invisibly).
